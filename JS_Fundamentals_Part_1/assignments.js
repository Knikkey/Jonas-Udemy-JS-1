/*
const country = 'USA';
const continent = 'North America';
let population = '329.5';
const isIsland = false;
// console.log(typeof isIsland);
const language = 'English';

console.log(isIsland);
console.log(population);
console.log(country);
console.log(language);

console.log(population / 2);
let finlandPopulation = 6;
console.log(population > finlandPopulation);
let averagePopulation = 33;
console.log(population > averagePopulation);
let description = 'USA is in North America, and its 329.5 million people speak English.';
console.log(description);

console.log(`${country} is in ${continent}, and its ${population} million people speak ${language}`);

const populationAverage = 33;

if (population > 33) {
    console.log(`${country}'s population is above average.`)
} else {
    console.log(`${country}'s population is ${population - populationAverage} million below the average.`);
}

// const numNeighbors = Number(prompt('How many neighbor countries does your country have?'))
// if (numNeighbors == 1) {
//     console.log('Only 1 border!')
// } else if (numNeighbors > 1) {
//     console.log('More than 1 border')
// } else {
//     console.log('No borders')
// }


if (language === 'English' && population < 50 && !isIsland) {
    console.log(`Come to ${country}, Sarah!`)
} else {
    console.log(`Maybe ${country} isn't for you...`)
}

switch (language) {
    case 'Chinese' || 'Mandarin':
        console.log('MOST number of native speakers!');
        break;
    case 'Spanish':
        console.log('2nd place in number of native speakers');
        break;
    case 'English':
        console.log('3rd place');
        break;
    case 'Hindi':
        console.log('Number 4');
        break;
    case 'Arabic':
        console.log('5th most spoken language');
        ; break
    default:
        console.log('Great language!');
        break;
}


console.log(`USA's population is ${population > 33 ? 'above' : 'below'} average.`);
*/

